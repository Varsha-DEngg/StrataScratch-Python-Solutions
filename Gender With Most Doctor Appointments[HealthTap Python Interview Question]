--Gender With Most Doctor Appointments[HealthTap Python Interview Question]

import pandas as pd
count_gender = medical_appointments.groupby(['gender'])['appointmentid'].count().reset_index().sort_values('appointmentid',ascending=False).head(1)
