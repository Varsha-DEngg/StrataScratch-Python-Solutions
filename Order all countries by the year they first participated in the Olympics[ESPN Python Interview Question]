--Order all countries by the year they first participated in the Olympics[ESPN Python Interview Question]

import pandas as pd
olympics_athletes_events.groupby(['noc'])['year'].min().to_frame('first_time_year').reset_index().sort_values(['first_time_year','noc'])
